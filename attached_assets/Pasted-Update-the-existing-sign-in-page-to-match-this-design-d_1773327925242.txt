Update the existing sign-in page to match this design direction exactly:
Background: Pure black (#0a0a0a). Add a radial gradient glow behind the logo — radial-gradient(circle, rgba(0,230,118,0.18) 0%, transparent 70%), positioned centered above the card.
Logo: Keep the existing flame logo as-is (rounded square, green border, green flame icon). Below the logo, add a small subtitle text "Welcome back" in #888, font-size 15px.
Card: Positioned at the bottom of the screen, border-radius: 32px 32px 0 0, background #161616, with a top border 1px solid #2a2a2a. Padding 32px 28px 40px.
Heading: "Sign In" in bold 26px, followed by a paragraph "Enter your details to continue" in #888, 14px.
Input fields: Background #1f1f1f, border 1.5px solid #2a2a2a, border-radius 14px, padding 16px 18px. On focus, border becomes #00E676 with a soft glow: box-shadow: 0 0 0 3px rgba(0,230,118,0.1). Placeholder text color #555.
Password field: Replace any existing eye toggle with this SVG eye icon (no emoji): <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#666" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>. Position it absolutely inside the field, right side, vertically centered. Toggle between this open-eye icon and a closed-eye version on click.
Forgot password: Right-aligned, color #00E676, font-size 13px, below the password field.
CTA button: Full width, background #00E676, color #000, border-radius: 50px, padding 17px, font-weight 700, font-size 16px, label "Continue →". Add box-shadow: 0 8px 24px rgba(0,230,118,0.3).
Divider: A horizontal rule with "or continue with" text centered between two lines, color #555, lines in #2a2a2a.
Social buttons: Two buttons side by side — Google and Apple. Background #1f1f1f, border 1.5px solid #2a2a2a, border-radius 14px, padding 14px. Each shows the respective brand SVG icon + label text in white.
Bottom line: "Don't have an account?" in #666 + "Create one" in #00E676, centered, font-size 14px.
Do not change any backend logic, routing, or authentication code — only update the visual styles and layout of the sign-in screen.